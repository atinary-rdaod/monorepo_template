# evaluation

Placeholder package. Its only job in the template is to show that the
`ml/` uv workspace can hold more than one member (here: `algorithm` and
`evaluation`), each with its own deployment story — `algorithm` ships as
a container, `evaluation` stays offline.

Fill it in with whatever evaluation pipeline you need.
