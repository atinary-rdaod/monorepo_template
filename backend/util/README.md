# util

Shared utilities that don't belong to any single deployable unit. Keep this
package small and dependency-light — anything heavier should live in the
project that actually uses it.

Imported as an editable workspace dependency from both `django` and `ml/algorithm`.
