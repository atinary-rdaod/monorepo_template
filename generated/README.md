# generated/

Code-generation artefacts that cross the language boundary. **Not committed**
— this directory is gitignored (apart from `.gitkeep` and this README).
Treat the files in here as derived state: regenerated by `just setup` and by
`just generate_types`, never edited by hand.

| File                  | Produced by                                    | Consumed by              |
| --------------------- | ---------------------------------------------- | ------------------------ |
| `openapi-schema.yml`  | `just backend django _generate_openapi_schema` | `frontend/` (npm openapi)|

Regenerate everything via `just generate_types` from the repo root.
