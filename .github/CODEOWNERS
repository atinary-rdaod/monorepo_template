# CODEOWNERS — keep ownership explicit so reviews land with the right people.
#
# Conventions used at the talk's source company:
#   * Every directory has at least one owner.
#   * Every directory has at least three owners so reviews are never blocked
#     when one teammate is on holiday.
#   * The closer a rule appears to the bottom, the higher its priority — list
#     the broad rules first, then the targeted overrides.
#
# Fill the placeholders below with your team's GitHub handles or teams.

# Default owners (apply everywhere unless a more specific rule below wins).
# *           @your-org/maintainers

# Infrastructure & CI
# /.github/          @your-org/platform
# /infrastructure/   @your-org/platform

# Backend
# /backend/django/   @your-org/backend
# /backend/util/     @your-org/backend
# /backend/typings/  @your-org/backend

# ML workspace
# /backend/ml/       @your-org/ml

# Frontend
# /frontend/         @your-org/frontend

# Note: `generated/` and `frontend/src/generated/` are gitignored (derived
# state regenerated by `just generate_types`), so they don't appear in PRs
# and don't need a CODEOWNERS entry.
